# 🎬 欢迎使用 JianYing-AI-Pro (剪映 AI 全自动生产力工具箱)

> **专为剪映专业版（JianYing Pro）深度定制的高效自动化利器**  
> 融合 **Google Gemini 多模态视觉大模型** 与 **高精度本地 RPA 自动化技术**，彻底告别重复繁琐的人工操作，让批量渲染导出、字幕工程清洗、跨草稿字体统一变得前所未有的简单与高效！

---

## 🌟 核心功能一览

### 1. 🤖 AI 视觉驱动 · 全自动无人值守批量导出
- **大模型智能寻路**：借助 Gemini 视觉多模态能力，一键秒级解析剪映首页草稿矩阵，精准识别草稿位置与标题，免除手动配准烦恼。
- **全自动化连续调度**：自动依次点开草稿、等待工程加载、触发导出配置、一键确认渲染，实现全流程无人值守。
- **双导出流模式支持**：
  - **模式 A（后台极速收起）**：导出开始后等待“收起至首页”按钮点亮，智能一键收起至后台渲染，立刻切入下一个草稿（最省时方案）。
  - **模式 B（逐个完整渲染）**：等待渲染 100% 完毕后自动关闭弹窗、返回首页，严谨闭环。
- **高精度双重视觉锁定**：结合 **OpenCV 图片模板匹配** 与 **微米级局部多点像素特征采样**，精准识别按钮禁用与就绪状态，彻底杜绝提前误点。

---

### 2. 📝 工业级字幕工具箱 · 一键工程重构
- **🎙️ 批量无损字幕提取（生成 .srt）**：
  - 一键扫描所有指定草稿，毫秒级解析底层 `draft_content.json`，提取全部文本与精确时间轴。
  - 自动输出标准的 UTF-8 `.srt` 外挂字幕文件至桌面。
  - 提取后可自动抹除原草稿文字轨道，实现一键制作“纯净无字幕母版”。
- **🔤 全局字体自动统一（Noto Sans Bold）**：
  - 针对剪辑工程字体不统一的痛点，内置自适应正则扫描引擎，自动穿透 Windows 系统字体目录与个人 AppData 字体库匹配正确的 `NotoSansBold` 路径。
  - 一键无感替换目录下所有草稿的字体配置，保持视觉规范绝对统一。
- **👁️ 批量解除字幕轨道隐藏**：
  - 强力穿透修复草稿轨道属性，一键恢复所有草稿中被误隐藏/关闭眼睛图标的文字轨道。
- **🔒 安全无锁写入**：
  - 采用严密的上下文安全文件句柄管理，自动生成并同步更新 `.bak` 备份文件，杜绝文件损坏与锁冲突。

---

### 3. 🏷️ 批量智能重命名
- 支持快速对导出的成品视频按命名规则或序号进行批量规范重命名，告别手动改名烦恼。

---

### 4. ⚙️ 自由灵活的引擎配置与直观体验
- **多模型自由切换**：支持 `Gemini 3.5 Flash`、`Gemini 2.5 Flash` 等多款主流大模型，自由平衡速率与配额。
- **快捷 API 管理**：界面直达 Google AI Studio 获取免费 Key，中文汉化所有常见网络与配额报错（如 429 配额用尽、403 权限提示等）。
- **实时广播双日志**：控制台输出无延迟同步至主界面与字幕工具箱，每一步操作尽收眼底。
- **云端热更新与安全守护**：支持版本自检、静默安全热升级及云端远程授权管控。

---

## 🚀 极速上手指南

```
[ 1. 扫描首页 ] ──> [ 2. 引导录制(按F8) ] ──> [ 3. 选取起始工程 ] ──> [ 4. 点击开始导出 ]
```

1. **环境准备**：打开剪映专业版，停留在**项目草稿首页**。
2. **AI 扫描**：点击软件中的 `AI 识别首页草稿`，等待队列自动填满草稿列表。
3. **坐标向导（仅首次需设置）**：
   - 点击 `引导录制`，按照状态栏绿色提示依次按下 **F8**：
     - **第 1 步**：点开任意草稿，鼠标移至右上角【导出】→ 按 **F8**；
     - **第 2 步**：鼠标移至弹出窗口的【确认导出】按钮 → 按 **F8**；
     - **第 3 步**：鼠标移至【收起至首页】（或完成后的【关闭】）背景处 → 按 **F8**；
     - **第 4 步**：如果第 3 步用了收起至首页，直接按 **F9 跳过**；若第 3 步是关闭，鼠标移至剪辑界面左上角【关闭草稿】→ 按 **F8**。
4. **一键开跑**：在表格中点击想要开始的第一个草稿，点击 `开始批量导出`，接下来请泡杯咖啡，静待全部出片！

---

## 💡 注意事项与使用建议

> [!TIP]
> - **保持剪映窗口前台**：自动化批量点击期间，请不要遮挡或最小化剪映窗口，避免模拟鼠标误触其他窗口。
> - **关于字幕工具操作**：在执行“统一字体”或“解除隐藏”等直接修改草稿配置的操作时，**请务必确保剪映处于首页关闭草稿状态**，以防剪映内存数据覆盖本地修改。
> - **多屏缩放提示**：Windows 系统显示缩放推荐保持标准比例，如遇高分辨率屏，内置的自适应特征探测亦可完美兼容。

---

祝使用愉快，大幅解放双手，高效产出！🎉
